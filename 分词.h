#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include<sstream>
#include <algorithm>
#include <map>
#include <vector>
#include<tuple>

using namespace std;

void 分词(string str);
void test07返回(string& c字符串引用, long long& num字数引用);
void  分词(string str);
string 读取文件查找内容(string chastr);